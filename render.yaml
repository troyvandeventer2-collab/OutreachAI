services:
  - type: web
    name: outreachai
    env: node
    buildCommand: cd client && npm install && npm run build
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
